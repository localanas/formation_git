name: SSH Test Azure VM

on:
  workflow_dispatch:

jobs:
  ssh:
    runs-on: ubuntu-latest
    environment: Production  # <-- mettre le nom exact de ton environment si le secret est là

    steps:
      - name: SSH into Azure VM
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: 4.233.147.103
          username: anasnadi
          key: ${{ secrets.SSH_KEY }}
          script: |
            echo "✅ SSH from GitHub OK"
            hostname
            whoami
